import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";

export async function GET(request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorised"},{status:401});const profileId=new URL(request.url).searchParams.get("profileId");let query=supabase.from("coaching_sessions").select("*").eq("user_id",user.id).order("created_at",{ascending:false});if(profileId)query=query.eq("profile_id",profileId);const {data,error}=await query;return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json(data)}
export async function POST(request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorised"},{status:401});const body=await request.json();const {data,error}=await supabase.from("coaching_sessions").insert({user_id:user.id,profile_id:body.profileId||null,job_title:body.analysis?.jobTitle||null,company:body.analysis?.company||null,source_resume:body.resume,job_description:body.jobDescription,analysis:body.analysis,follow_up_answers:body.answers,positioning:body.positioning,generated_resume:body.documents?.resume,cover_letter:body.documents?.coverLetter,writers_notes:body.documents?.writersNotes,documents:body.documents||{}}).select().single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json(data,{status:201})}
